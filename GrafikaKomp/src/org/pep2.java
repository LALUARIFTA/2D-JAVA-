/*
 * 
 * bunga
 * dedy
 */
package org;

import java.awt.Color;
import java.awt.Graphics;
import javax.swing.JFrame;

/**
 *
 * @author gawah
 */
public class pep2 extends JFrame {
    public pep2() {
        setSize(600, 600);
        setLocationRelativeTo(null);
        setVisible(true);
    }

    public void b1(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 100;       //y1 = y1 + 100
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 75;        //w1 = w1 + 100
        int q2 = 30;
        int w2 = 80;
        
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.red);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.blue);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
    }

    public void b2(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 200;
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 175;
        int q2 = 30;
        int w2 = 80;
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }

    }

    public void b3(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 300;
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 275;
        int q2 = 30;
        int w2 = 80;
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }

    }

    public void b4(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 400;
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 375;
        int q2 = 30;
        int w2 = 80;
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }

    }

    public void b5(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 500;
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 475;
        int q2 = 30;
        int w2 = 80;
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }

    }

    public void b6(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 600;
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 575;
        int q2 = 30;
        int w2 = 80;
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }

    }

    public void b7(Graphics g) {

        int x = 17;
        int x1 = 0;
        int y1 = 700;
        int x2 = 80;
        int y2 = 30;

        int q1 = 25;
        int w1 = 675;
        int q2 = 30;
        int w2 = 80;
        while (x >= 1) {
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }
        while (x >= 1) {
            x1 = x1 + 0;
            y1 = y1 + 10;
            w1 = w1 + 10;
            x1 = x1 + 80;
            q1 = q1 + 80;
            x = x - 1;
            g.setColor(Color.RED);
            g.fillOval(x1, y1, x2, y2);
            g.setColor(Color.BLUE);
            g.fillOval(q1, w1, q2, w2);
        }

    }

    public void border(Graphics g) {    
        g.drawLine(80, 70, 1440, 70);        //atas
        g.drawLine(80, 760, 1440, 760);      //atas
        g.drawLine(80, 70, 80, 760);         //kiri
        g.drawLine(1440, 70, 1440, 760);     //kanan

    }

    public void paint(Graphics g) {

        b1(g);
        b2(g);
        b3(g);
        b4(g);
        b5(g);
        b6(g);
        b7(g);
        border(g);
    }

    public static void main(String[] args) {
        pep2 gambar = new pep2();
        gambar.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}


